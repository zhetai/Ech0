apiVersion: v1
kind: Service
metadata:
  name: {{ include "ech0.fullname" . }}
  labels:
    {{- include "ech0.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ech0.selectorLabels" . | nindent 4 }}
