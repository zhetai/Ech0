apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ech0.fullname" . }}-test-connection"
  labels:
    {{- include "ech0.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ech0.fullname" . }}:{{ .Values.service.httpPort }}']
  restartPolicy: Never
